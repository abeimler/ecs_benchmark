# Scale font and line width (dpi) by changing the size! It will always display stretched.
set terminal png size 1024,800 enhanced font "Arial,14"
set output '{{output}}'

# Key means label...
set key inside bottom right
set xlabel '{{xlabel}}'
set ylabel '{{ylabel}}'
set title '{{title}}'

set logscale x 2
set logscale y 10
set format x "2^{%L}"
set format y "10^{%L}"

## the labels are in Alphabetical order
plot {{#headers}} "{{data_dat}}" using 1:{{col}} title '{{header}}' with lines lw 3 {{^-last}}, {{/-last}} {{/headers}}
